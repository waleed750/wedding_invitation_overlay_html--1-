# Wedding Invitation

Static wedding invitation page with an overlay map button.

## Deploy

This project is ready for GitHub Pages. Push it to a public GitHub repository and the included workflow will publish the site from `main`.

It can also be imported directly into Vercel as a static project.
